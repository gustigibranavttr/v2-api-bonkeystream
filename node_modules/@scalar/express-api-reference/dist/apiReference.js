import { renderApiReference } from '@scalar/client-side-rendering';
/**
 * The default configuration for the API Reference.
 */
const DEFAULT_CONFIGURATION = {
    _integration: 'express',
};
/**
 * The route handler to render the Scalar API Reference.
 */
export function apiReference(givenConfiguration) {
    // Merge the defaults
    const configuration = {
        ...DEFAULT_CONFIGURATION,
        ...givenConfiguration,
    };
    // Respond with the HTML document
    return (_, res) => {
        const { cdn, pageTitle, nonce, ...config } = configuration;
        res.type('text/html').send(renderApiReference({ config, pageTitle, cdn, nonce }));
    };
}
